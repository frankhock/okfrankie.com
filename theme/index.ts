import { deep } from '@theme-ui/presets'

export const theme = {
  ...deep,
  fonts: {
    ...deep.fonts,
    heading: 'Merriweather',
  },
  lineHeights: {
    ...deep.lineHeights,
    heading: 1.375,
  },
}
